# Engage 360 — Auth Testing Notes

No authentication is required for any route or API in Phase 0 or Phase 1.
All endpoints are accessible directly without login. Use any browser session.

The /api/me endpoint returns a hardcoded user/tenant payload for the UI to render.
